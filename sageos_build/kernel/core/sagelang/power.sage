// ============================================================================
// Power Driver (SageLang)
// ============================================================================

fn power_qemu_exit_driver() {
    power_qemu_exit()
}

fn power_reboot_driver() {
    power_reboot()
}

fn power_shutdown_driver() {
    power_shutdown()
}
